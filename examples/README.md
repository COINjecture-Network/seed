# GoldenSeed Examples

This directory contains examples and demonstrations of GoldenSeed's capabilities.

## 🎬 Interactive Demos

### Alice ↔ Bob Data Teleportation Demo

**[alice_bob_teleportation_demo.html](alice_bob_teleportation_demo.html)** - A self-contained interactive HTML demo that visualizes the classical data teleportation concept:

- Alice sends a tiny seed (Φ - the golden ratio symbol) to Bob
- Bob receives the seed and generates an infinite data stream
- Demonstrates how 32 bytes can generate unlimited deterministic data
- **Usage**: Open the HTML file in any modern web browser (no dependencies required)

![Alice Bob Demo](https://github.com/user-attachments/assets/2b143df8-edb0-4b1f-91aa-c42afe855467)
*Initial state: Ready to demonstrate data teleportation*

![Alice Bob Demo Active](https://github.com/user-attachments/assets/954178e6-f2dd-45c7-a59c-3d595d3c1035)
*Active state: Bob generating infinite data from the seed*

---

## 🔧 Binary Fusion Tap - Multi-Language Examples

This section contains production-ready implementations of the Binary Fusion Tap algorithm in 7 programming languages, generated by the multi-language compiler.

## Generated Implementations

All implementations produce identical output for k=11:

```
K Parameter: 11
Seed Value: 1234567891011
Tap State: 0b10001111101110001111110110000100001000100011
ZPE Overflow: 0b111011
```

### Available Languages

| File | Language | Run Command |
|------|----------|-------------|
| `binary_fusion_tap.py` | Python | `python3 binary_fusion_tap.py` |
| `binary_fusion_tap.js` | JavaScript | `node binary_fusion_tap.js` |
| `binary_fusion_tap.ts` | TypeScript | `tsc binary_fusion_tap.ts && node binary_fusion_tap.js` |
| `binary_fusion_tap.rs` | Rust | `rustc binary_fusion_tap.rs && ./binary_fusion_tap` |
| `binary_fusion_tap.go` | Go | `go run binary_fusion_tap.go` |
| `binary_fusion_tap.c` | C | `gcc binary_fusion_tap.c -o bft && ./bft` |
| `binary_fusion_tap.java` | Java | `javac BinaryFusionTap.java && java BinaryFusionTap` |

## Regenerating Examples

To regenerate all examples from the compiler:

```bash
cd ..
python3 language_compiler.py --all
```

To generate a specific language:

```bash
python3 language_compiler.py --language rust -o custom_name.rs
```

## Algorithm Details

Each implementation includes:

1. **Seed Generation** - Concatenate numbers 1 through k
2. **8-fold Heartbeat** - Bit-shift left by 3 (equivalent to × 8)
3. **Phase Offset** - Add k parameter
4. **ZPE Overflow** - XOR extraction for k ≥ 10

## Integration

These examples can be directly integrated into your projects:

- **Python**: Import as module
- **JavaScript/TypeScript**: Use as ES6 module
- **Rust**: Include as library crate
- **Go**: Import as package
- **C**: Compile and link
- **Java**: Use as class

## License

Same license as the main COINjecture protocol.
